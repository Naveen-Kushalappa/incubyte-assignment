const assert = require('assert');
describe('Test suite', function () {
    describe('Health check', function () {
        it('Check health of the test suite', function () {
            assert.equal(true, true);
        });
    });
});